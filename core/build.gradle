plugins {
    id "io.spring.dependency-management" version "1.0.5.RELEASE"
}

jar {
    baseName = 'proper-his-core'
}

dependencyManagement {
    imports { mavenBom("org.springframework.boot:spring-boot-dependencies:${springBootVersion}") }
}

dependencies {
}
